import React from 'react';
import Home from './pages/Home.jsx';

// Stap 2: één scherm — inloggen met Spotify en je profiel zien.
// Volledige routing (Setup, Lobby, Host, Play, Score, Admin) komt in de
// volgende stappen.
export default function App() {
    return <Home />;
}
